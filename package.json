{
  "name": "learn-framework",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/dedent": "^0.7.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vitest": "^0.16.0"
  },
  "dependencies": {
    "dedent": "^0.7.0"
  }
}
